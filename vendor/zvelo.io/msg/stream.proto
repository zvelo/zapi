syntax = "proto3";

package zvelo.msg;

option go_package = "zvelo.io/msg";
import "zvelo/msg/dataset.proto";

message StreamResult {
  string url = 1;
  DataSet dataset = 2;
}

message StreamParams {
}

service Stream {
  rpc Events(StreamParams) returns (stream StreamResult) {}
}
